spring:
  application:
    name: banana-zuul
  cloud:
    config:
      name: eureka,admin,redis
      uri: http://192.168.26.128:10010
server:
  port: 80
zuul:
  routes:
    BANANA_AUTH:
      path: /auth/**
      serviceId: banana_auth